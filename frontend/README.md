# Auto Shop Frontend (Vite + React)

## Setup
1. `cd frontend`
2. `npm install`
3. Create `.env` from `.env.example` or set `VITE_API_BASE`
4. Run: `npm run dev`

## Pages
- Home, Services, Book (with booking form), Admin (read-only list of bookings)
